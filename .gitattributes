tests/fixtures/*.seg.jsonl text eol=lf
contracts/abbey/** -text
# Binary HTTP wire fixture: preserve CRLF and the terminal empty header line.
tools/abbey-audio-tap/Tests/AudioTapCoreTests/Fixtures/stream-header.http -text -diff
# command_catalog's readme test include_str!s this file and compares the
# generated region byte for byte with LF output; an autocrlf checkout
# (the Windows runner) must not rewrite it.
README.md text eol=lf
# Managed protocol golden bytes end in one LF on every platform.
tests/fixtures/service-protocol/*.json text eol=lf
# Consent serialization is also compared byte for byte after its LF.
tests/fixtures/voice_consent_v1.json text eol=lf
# episode_gate/tests.rs compares serde_json::to_string output with these
# fixtures byte for byte (after trim_end); pin LF so an autocrlf checkout
# cannot pass today only because each fixture happens to be one line.
tests/fixtures/episode_write_*.json text eol=lf
