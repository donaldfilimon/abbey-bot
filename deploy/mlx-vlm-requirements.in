# Resolved with hashes into mlx-vlm-requirements.txt for Apple Silicon macOS.
mlx-vlm==0.6.15
# Gemma 4's Transformers chat template is Jinja-based, but mlx-vlm does not
# declare this runtime dependency. The real text smoke fails closed without it.
jinja2==3.1.6
